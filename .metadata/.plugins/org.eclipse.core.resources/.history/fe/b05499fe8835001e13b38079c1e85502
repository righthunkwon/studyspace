package array02;

public class Array02_03_SelectionSort {
	public static void main(String[] args) {
		int[] arr = { 64, 25, 10, 22, 11 };
		int n = arr.length;

		// 선택 정렬은 n - 1 만큼의 사이클의 정렬을 거친다.
		for (int i = 0; i < n - 1; i++) {
			int minIdx = i; // 최소값을 가진 인덱스를 초기화

			// 최소값을 앞에서부터 정렬하는 반복문
			for (int j = i + 1; j < n; j++) {
				if (arr[minIdx] > arr[j]) {
					minIdx = j;
				}
			}
			
			int tmp = arr[i];
			arr[i] = arr[minIdx];
			arr[minIdx] = tmp;
		}
	}

}
