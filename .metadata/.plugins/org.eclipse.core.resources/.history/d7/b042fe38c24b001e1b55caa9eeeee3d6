<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>01_get_post</title>
</head>
<body>

	<h2>GET Form</h2>
	<!-- action: 요청을 보내는 경로(default: 자기 자신 -->
	<!-- method: 요청을 보내는 방식(default: get) -->
	<!-- html에서는 대소문자를 구별하지 않기 때문에 GET을 get으로 적어도 된다. -->
	<form action="" method="GET"> 
		<!-- name : key에 해당 -->
		<!-- value: name에 작성된 값 -->
		<input type="text" name="id">
		<!-- form tag 내부의 button은 따로 type을 submit으로 지정하지 않더라도 submit의 기능을 한다. -->
		<input type="submit">
	</form>
	
	<h2>POST Form</h2>
	<form action="" method="POST">
		<input type="text" name="id"> 
		<input type="submit">
	</form>
	
	<!-- /의 차이를 느껴라! -->
	<!-- / 존재: Context root를 대체한다. -->
	<!-- / 없음: Context root에 붙는다. -->
	
	<!-- 자바에서의 /는 직전 / 이후에 요소를 붙게 하여 기존재 존재하던 context root 등이 살아있지만, -->
	<!-- HTML에서의 /는 port 번호 이후에 경로를 붙게 하여 기존에 존재하던 context root등 을 삭제한다. -->
	<h2>Action1</h2>
	<!-- /가 존재하면 Context root가 사라지고 그 자리에 gogo가 붙는다.. -->
	<form action="/gogo" method="GET">
		<!-- name 속성필요. -->
		<input type="text" name="id">
		<input type="password" name="pw">
		<input type="submit">
	</form>
	
	<h2>Action2</h2>
	<!-- /가 없으면 Context root를 유지하고 이후에 gogo 경로가 추가된다.. -->
	<form action="gogo" method="GET">
		<!-- name 속성필요. -->
		<input type="text" name="id">
		<input type="password" name="pw">
		<input type="submit">
	</form>
</body>
</html>